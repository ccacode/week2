void setup(){
  background (0);
  size(500,500);
  frameRate(15);
}

void draw(){
  popdot();
}
void popdot(){
  int x=20;
  while(x<480){
    noStroke();
    fill(random(250),random(240),random(230));
    ellipse(mouseX,mouseY,35,35);
    x=x+30;
  }
}
